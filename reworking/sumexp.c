#include <stdio.h>
#include <stdlib.h>
#include <gmp.h>

// Compile Notes: use `gcc <program>.c -lgmp`

mpz_t num;

int main(){
    
    // From docs, this assigns num the value of (op1*2) ^ op2
    mpz_mul_2exp(num, )
    return 0;
}

